
<div class="main" >
<h3><i class="fa fa-car" aria-hidden="true"></i> ABC de Vehiculos </h3>

 <div class="w-50 p-3 mx-auto" >
        <div class="input-group mb-3 ">
            <input type="search" id="buscar" placeholder="Buscar vehiculo (id, marca, tipo, modelo, no.serie, placas) . . . ." class="form-control" >
            <div href="#" title="Buscar Vehiculos" id="btnBuscar" class="btn btn-info"><i class="fa fa-search" aria-hidden="true"></i></div>
        </div>
</div>

{{#if vehiculos}}
<div class="table">
    <div class="row">
            <div class="col mx-auto" align="right">
                <table  id="MyTable" class="table-responsive-lg table-striped table-bordered table-hover" cellspacing="0">
                    <thead class="thead-dark" align="center">
                        <th>Cliente</th>
                        <th>Id Vehiculo</th>
                        <th >Marca</th>
                        <th >Tipo</th>
                        <th>Color</th>
                        <th>Modelo</th>
                        <th>Serie</th>
                        <th>Placas</th>
                        <th>Acciones</th>
                        
                    </thead>
                    <tbody>
                        {{#each vehiculos}}
                        <tr>
                            <!--<td>{{ nombre }}</td>-->
                            <td><a href="/vehiculos/dis/{{ id_cliente }}" onclick="" title="Vehículos de Cliente">{{ nombre }} </td>
                            <td>{{ id_veh }}</td>
                            <td>{{ marca }}</td>
                            <td>{{ tipo }}</td>
                            <td>{{ color }}</td>
                            <td>{{ modelo }}</td>
                            <td>{{ serie }}</td>
                            <td>{{ placas }}</td>
                            <td align="center"> 
                            
                            <a href="/vehiculos/edit/{{ id_veh }}" onclick="" title="Editar registro"> 
                            <span class="material-icons md-24">edit_note</span>
                            </a>    
                        
                            <a href="#" onclick="return EliminaRegistro({{id_veh}}, 'vehiculo', )" title="Borrar Registro">
                            <span class="material-icons md-24">delete_outline</span></a>                          
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

{{/if}}

<script>
    const buscar = document.querySelector('#buscar');
    const btnBuscar = document.querySelector('#btnBuscar');
  
    const busca = () => {
    const valor_busqueda = buscar.value

    if (valor_busqueda.length > 0) {
        window.location="/vehiculos/search/"+[valor_busqueda];
    } else { 
         var notification = alertify.notify('La busqueda debe de contener almenos un caracter', 'success', 5, function(){  console.log('dismissed'); });
    Run
    }   

    } 

    //buscar.addEventListener('enter', busca)
    btnBuscar.addEventListener('click', busca)
</script>
 


